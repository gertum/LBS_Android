<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.os_app_gertum1">

    <!-- Permission declarations (if needed) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- MainActivity as the launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MapActivity -->
        <activity android:name=".ui.map.MapActivity"
            android:label="Map" />

        <!-- SignalStrengthListActivity -->
        <activity android:name=".ui.signalstrengthlist.SignalStrengthListActivity"
            android:label="Signal Strengths" />

        <!-- UserIdentificationActivity -->
        <activity android:name=".ui.useridentification.UserIdentificationActivity"
            android:label="User Identification" />

        <!-- UserIdentificationActivity -->
        <activity android:name=".ui.editsignalstrength.EditSignalStrengthActivity"
            android:label="Edit Signal Strength" />

    </application>

</manifest>
